/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true
    },
    basePath: '/Obelisco-V2',
    assetPrefix: '/Obelisco-V2',
};

export default nextConfig;
